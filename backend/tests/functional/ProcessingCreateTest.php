<?php

use backend\models\Processing;
use Yii;

class ProcessingCreateTest extends \Codeception\Test\Unit
{
    /**
     * @var \FunctionalTester
     */
    /*
    protected $tester;

    protected function _before()
    {
    }

    protected function _after()
    {
    }

    // tests
    public function testProcessingCreate()
    {
        $faker = \Faker\Factory::create();

        $source = new Processing();

        $source->tour          = $faker->url();
        $source->date          = $faker->url();
        $source->team_home     = $faker->url();
        $source->team_away     = $faker->url();
        $source->score         = $faker->url();
        $source->score_first   = $faker->url();
        $source->stadium       = $faker->url();
        $source->count_peoples = $faker->url();
        $source->main_players_home   = $faker->url();
        $source->main_players_away   = $faker->url();
        $source->reserv_players_home = $faker->url();
        $source->reserv_players_away = $faker->url();
        $source->player_name   = $faker->url();
        $source->player_number = $faker->url();
        $source->coach_home    = $faker->url();
        $source->coach_away    = $faker->url();
        $source->yellow_card   = $faker->url();
        $source->red_card      = $faker->url();
        $source->yel_red_card  = $faker->url();
        $source->penalty_yes   = $faker->url();
        $source->penalty_no    = $faker->url();
        $source->autogoal      = $faker->url();
        $source->country       = $faker->url();
        $source->out_match     = $faker->url();
        $source->in_match      = $faker->url();
        $source->match_events  = $faker->url();
        $source->goal          = $faker->url();
        $source->weather       = $faker->url();
        $source->statistics    = $faker->url();

        $this->assertNotNull($this->tester);
        $this->assertFalse(!$source->save(),'==========>>> Error save! <<<===========');        
    }
    */
}